---
category: Base UI
---

Toast outlet (sonner) — mount once near the app root; fire toasts via the sonner toast() API. Theme-aware via next-themes.

## Usage

```tsx
<Toaster position="bottom-right" />
```
